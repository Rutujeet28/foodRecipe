const getRecipes = (req, res) => {            // defines a controller function getRecipes with the request, response objects
    res.json({message : "hello"})
}

const getRecipe = (req, res) => {            // defines a controller function getRecipes with the request, response objects
    res.json({message : "hello"})
}

const addRecipe = (req, res) => {            // defines a controller function getRecipes with the request, response objects
    res.json({message : "hello"})
}

const editRecipe = (req, res) => {            // defines a controller function getRecipes with the request, response objects
    res.json({message : "hello"})
}

const deleteRecipe = (req, res) => {            // defines a controller function getRecipes with the request, response objects
    res.json({message : "hello"})
}

module.exports = {getRecipes, getRecipe, addRecipe, editRecipe, deleteRecipe}                //makes the function usable in other files